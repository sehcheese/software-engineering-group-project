<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#FFFFFF"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Vibrator" >

    <TextView
        android:id="@+id/parking_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Covenant College Parking"
        android:textColor="@android:color/black"
        android:textSize="20sp" />

    <ListView
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:id="@+id/parking_notifications"
        android:layout_below="@+id/parking_header"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/parking_regulations"
        android:textAllCaps="false"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Parking Regulations"
        android:layout_marginTop="20dp"
        android:layout_below="@+id/parking_notifications"
        android:layout_centerHorizontal="true" />

</RelativeLayout>